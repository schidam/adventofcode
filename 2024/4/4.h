#include <iostream>
#include <string>
#include <vector>
#include <unordered_map>

struct Puzzle {
    int solveA(std::string);
    int solveB(std::string);
    bool solveX(std::string, std::string);
};
